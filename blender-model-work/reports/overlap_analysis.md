# 引风机对象重叠与重复检测

> 本报告只比较隔离副本中的几何与变换，不删除对象，也不据此自动修改模型。

| 对象对 | 顶点顺序相同 | 拓扑相同 | 世界变换相同 | 包围盒相同 | 共面重叠 | 仅材质不同 | 完全重复 |
|---|---|---|---|---|---|---|---|
| SHELL / SHELL002 | 否 | 否 | 是 | 是 | 否 | 否 | 否 |
| SHELL001 / SHELL008 | 是 | 是 | 是 | 是 | 是 | 否 | 是 |
| SHELL009 / SHELL019 | 否 | 否 | 是 | 是 | 否 | 否 | 否 |
| SHELL010 / SHELL021 | 是 | 是 | 是 | 是 | 是 | 是 | 是 |
| SHELL020 / SHELL022 | 否 | 是 | 是 | 是 | 是 | 否 | 否 |
| SHELL024 / SHELL025 | 否 | 否 | 是 | 否 | 否 | 否 | 否 |

## 逐对结论

### SHELL / SHELL002

- 内外表面判断：可能：包围盒相同但顶点或拓扑不同，疑似内外表面；必须人工剖视确认
- 材质：`SHELL` = ['color_cccccc']；`SHELL002` = ['color_cccccc']
- 结论：不是完全重复对象；保留并等待人工判断

### SHELL001 / SHELL008

- 内外表面判断：否：世界坐标和面几何一致，不是可区分的内外表面
- 材质：`SHELL001` = ['color_c8c8c8']；`SHELL008` = ['color_c8c8c8']
- 结论：几何与世界位置完全重复；本轮仅记录，不删除

### SHELL009 / SHELL019

- 内外表面判断：可能：包围盒相同但顶点或拓扑不同，疑似内外表面；必须人工剖视确认
- 材质：`SHELL009` = ['color_cd843f']；`SHELL019` = ['color_cd843f']
- 结论：不是完全重复对象；保留并等待人工判断

### SHELL010 / SHELL021

- 内外表面判断：否：世界坐标和面几何一致，不是可区分的内外表面
- 材质：`SHELL010` = ['color_cd843f']；`SHELL021` = ['color_adb5bd']
- 结论：几何与世界位置完全重复；本轮仅记录，不删除

### SHELL020 / SHELL022

- 内外表面判断：无法认定为内外表面
- 材质：`SHELL020` = ['color_cd843f']；`SHELL022` = ['color_adb5bd']
- 结论：不是完全重复对象；保留并等待人工判断

### SHELL024 / SHELL025

- 内外表面判断：无法认定为内外表面
- 材质：`SHELL024` = ['color_cd843f']；`SHELL025` = ['color_cd843f']
- 结论：不是完全重复对象；保留并等待人工判断
